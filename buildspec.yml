version: 0.1
phases:
  install:
    commands:
      - npm install
      
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket bucketarun6969 --output-template-file outputSamTemplate.yaml --debug
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml